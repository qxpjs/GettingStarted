//Observe how we start a new line in the text shown in the alert using \n
var appInfo = app.name + " " + app.version + " " + " is installed in folder '" + app.dir + "'\nApp Language Code: '" + app.language.code + "'";

alert(appInfo);
console.log(appInfo);